{% extends 'base.html.twig' %}

{% block title %}Hello OrderController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>orders! ✅</h1>
        <ul>
            {% for order in orders %}
                <li>{{ order.id }} - {{ order.status }}</li>
                <a href="order/{{ order.id }}">bekijken</a>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
